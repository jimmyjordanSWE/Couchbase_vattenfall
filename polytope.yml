include:
  - repo: gh:bluetext-io/bluetext
  - services/couchbase-server
  - services/python-fast-api
  - services/react-web-app
  - services/service-config-manager
  - services/sync-gateway
  - services/couchbase-edge-server
tools:
  stack:
    run:
      - tool: load-config
      - tool: couchbase-server
      - tool: python-fast-api
      - tool: react-web-app
      - tool: service-config-manager
      - tool: sync-gateway
      - tool: "couchbase-edge-server"
  initialize-session:
    info: 'Initialize **EVERY** session by calling this tool.

      Returns required development context and ensures the project is running.

      This tool always needs to be called before doing anything else, regardless of the tasks complexity.'
    run:
      - id: get-general-context
        tool: get-dev-context
      - id: apply-stack-and-await-finish
        tool: stack
